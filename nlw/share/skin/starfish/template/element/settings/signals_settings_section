[%# vim: set et sts=2 sw=2: %]
<h3>[% loc('Signals') %]</h3>

[% INCLUDE element/settings/setting_select
  label=loc('Send Private Signals as Email')
  pref="plugin.signals.dm_sends_email"
  formats=plugin.signals.dm_sends_email
  user_setting=plugin.signals.user.dm_sends_email
%]

<div class="push_5 grid_7">[% loc("If 'Yes', each time a user sends you a private message via Signals, we'll send you an email with the message.") %]</div>
<div class="clear"></div>

<hr />
